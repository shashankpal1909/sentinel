// Package proxy implements request forwarding and reverse proxy functionality.
//
// It routes incoming requests to selected upstream backends and returns
// their responses back to client callers.
package proxy
