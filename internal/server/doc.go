// Package server manages the HTTP server lifecycle and request listeners for Sentinel.
//
// It sets up network listeners, gracefully handles server startup and shutdown,
// and orchestrates routing and proxy middleware.
package server
