// Package domain contains the core runtime models used by Sentinel.
// These types are transport-agnostic and represent the application's
// business entities.
//
// The domain package must not depend on HTTP, YAML parsing,
// configuration loading, or infrastructure packages.
package domain
